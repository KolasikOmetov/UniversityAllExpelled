<Window x:Class="UniversityAllExpelledWorkerView.StudentsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniversityAllExpelledWorkerView"
        mc:Ignorable="d"
        Title="Студенты" MinHeight="450" MinWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <DataGrid Grid.Column="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО" Width="*">
                </DataGridTextColumn>
                <DataGridTextColumn Header="Группа" Width="*">
                </DataGridTextColumn>
                <DataGridTextColumn Header="Номер зачетной книжки" Width="2*">
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <Button x:Name="ButtonAdd" Margin="10" Padding="5">Добавить</Button>
            <Button x:Name="ButtonUpd" Margin="10" Padding="5">Изменить</Button>
            <Button x:Name="ButtonDel" Margin="10" Padding="5">Удалить</Button>
        </StackPanel>
    </Grid>
</Window>
