<Window x:Class="UniversityAllExpelledWarehouserView.StatsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib" xmlns:oxy="http://oxyplot.org/wpf"
        mc:Ignorable="d"
        Title="Отчёт" Height="450" Width="1000" Loaded="StatsWindow_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Label Margin="10">С</Label>
            <DatePicker Margin="10" Name="datePickerFrom"/>
            <Label Margin="10">по</Label>
            <DatePicker Margin="10" Name="datePickerTo"/>
            <Label Margin="10">Дисциплина</Label>
            <ComboBox Margin="10" MinWidth="100" Name="ComboBoxSubject">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Margin="10" Click="ButtonShow_Click">Показать</Button>
            <Button Margin="10" Click="ButtonSendToMail">На почту</Button>
        </StackPanel>
    </Grid>
</Window>
