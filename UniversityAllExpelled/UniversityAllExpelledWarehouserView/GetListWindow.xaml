<Window x:Class="UniversityAllExpelledWarehouserView.GetListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniversityAllExpelledWarehouserView"
        mc:Ignorable="d"
        Title="Список студентов" Height="450" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Label>Преподаватель</Label>
            <ComboBox Margin="10"/>
        </StackPanel>
        <ListBox Grid.Row="1" ItemsSource="{Binding StudentViewModel}" Name="ListBoxStudents">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <CheckBox Content="{Binding Name}" IsChecked="{Binding IsChecked}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center">
            <Button Margin="10">Word</Button>
            <Button Margin="10">Excel</Button>
            <Button Margin="10">Отмена</Button>
        </StackPanel>
    </Grid>
</Window>
