<Window x:Class="UniversityAllExpelledWarehouserView.CheckListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UniversityAllExpelledWarehouserView"
        mc:Ignorable="d"
        Title="Ведомости" MinHeight="450" MinWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <DataGrid Grid.Column="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Преподаватель" Width="3*">
                </DataGridTextColumn>
                <DataGridTextColumn Header="Дата проведения" Width="*">
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <Button Margin="10" Padding="5">Добавить</Button>
            <Button Margin="10" Padding="5">Изменить</Button>
            <Button Margin="10" Padding="5">Удалить</Button>
        </StackPanel>
    </Grid>
</Window>
